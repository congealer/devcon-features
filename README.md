# Dev Container Features

이 repo는 custom devcontainer features를 공유하기 위한 것입니다.
[Devcontainer 공식 feature 저장소](https://containers.dev/features)에 없는 기능을 구현하기 위해 만들어졌습니다.
대부분의 feature는 Debian/Ubuntu와 같이 apt package manager system 위에서 작동하지만, `arm-gnu-toolchain`처럼 apt를 사용하지 않는 것들도 있습니다.

## Devcontainer에 feature 추가 방법
추가하려는 .devcontainer/devcontainer.json에 `feature` 항목에 필요한 feature의 URL과 옵션을 지정합니다. 아래는 `hello` feature를 사용하는 예시입니다.
각 feature들의 옵션은 `src` 아래 각 feature들의 `README.md`를 참조하세요.
```jsonc
{
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
    "features": {
        "ghcr.io/congealer/devcon-features/hello:1": {
            "greeting": "Hello"
        }
    }
}
```

## Testing Features
이 항목은 개발자들을 위한 내용입니다. 모든 내용은 이 repo에 정의되어 있는 devcontainer 위에서 실행해야 합니다.

### Makefile

테스트는 [Makefile](Makefile)로 실행하는 것이 가장 간단합니다. CLI에는 base image 같은 기본값을 파일에 두는 방법이 없어서(설정 파일 옵션 없음, 환경변수 무시, `--config` 미지원) 매번 되풀이되는 인자를 여기에 고정해 두었습니다.

| make 타깃 | 실행 내용 |
|---|---|
| `make test-<feature>` | 해당 feature 전체 (`test.sh` + 시나리오 + `duplicate.sh`) |
| `make unit-<feature>` | 해당 feature의 `test.sh`만 |
| `make scenario-<feature>` | 해당 feature의 시나리오만 |
| `make test-global` | 전역 시나리오만 |
| `make test` | 저장소 전체 |

각 타깃이 어떤 컨테이너를 만들고 그 안에서 무엇을 실행하는지는 아래 [테스트 실행 조합](#테스트-실행-조합)에 정리돼 있습니다.

`make` 또는 `make help`로 타깃 목록을 볼 수 있습니다. 없는 feature 이름을 주면 `src/` 목록과 대조해 CLI에 넘기기 전에 걸러냅니다.

기본값은 변수로 덮어씁니다.

```bash
make test-prezto BASE_IMAGE=mcr.microsoft.com/devcontainers/base:debian
make test REMOTE_USER=octocat
```

#### 테스트가 남긴 것 정리

테스트는 컨테이너를 지우지만 **이미지와 빌드 캐시는 남깁니다.** 몇 번만 돌려도 수 GB가 쌓입니다.

| 타깃 | 삭제 대상 |
|---|---|
| `make clean` | 테스트 컨테이너, 테스트 이미지, dangling 빌드 캐시 |
| `make distclean` | 위 전부 + 미사용 빌드 캐시 전체 + base image |

`distclean`은 devcontainer를 처음 열었을 때의 상태로 되돌립니다.

지우는 이미지는 `vsc-<타임스탬프>-<해시>-features` 형식만입니다. 개발 컨테이너 이미지는 폴더 이름 기반(`vsc-<폴더>-<해시>-features`)이라 걸리지 않습니다.

### 테스트 수동 실행

Makefile이 다루지 않는 조합이 필요할 때, 또는 옵션을 직접 조합해 볼 때 씁니다.

```bash
devcontainer features test -p <프로젝트 루트> [옵션...]
```

`<프로젝트 루트>`는 `src`와 `test` 디렉토리를 가진 디렉토리로, 보통 저장소 최상위(`.`)입니다.

프로젝트 루트를 마지막 위치 인자로 넘기는 형태(`devcontainer features test [옵션...] .`)도 동작하지만 **`--help`에서 deprecated로 표시되어 있고, `-f` 바로 뒤에 두면 오작동합니다.** `-f`는 값을 여러 개 받는 배열 옵션이라 뒤따르는 인자를 feature 이름으로 흡수합니다.

```console
$ devcontainer features test -f prezto .
>  features:          prezto, .          ← '.'을 feature 이름으로 인식
[-] Failed to launch container:
ENOENT: no such file or directory, open '.../.devcontainer/devcontainer-feature.json'
```

`-p`를 쓰면 이 문제가 없습니다.

| 옵션 | 설명 |
|---|---|
| `-p, --project-folder <경로>` | `src`와 `test`를 가진 프로젝트 루트. 기본값 `.` |
| `-f, --features <이름...>` | 테스트할 feature 지정. 생략하면 저장소의 모든 feature |
| `-i, --base-image <이미지>` | base image. 기본값 `ubuntu:focal` |
| `-u, --remote-user <유저>` | remoteUser. 생략하면 base image가 선언한 값이며 **root가 아닐 수 있습니다.** `devcontainers/base:*`는 `vscode`, 맨 distro 이미지(`ubuntu:focal` 등)는 `root` |
| `--skip-scenarios` | 시나리오를 제외 |
| `--skip-autogenerated` | 기본 테스트(`test.sh`)를 제외 |
| `--skip-duplicated` | `duplicate.sh`를 제외 |
| `--global-scenarios-only` | `test/_global/`의 시나리오만 실행. `-f`와 함께 쓸 수 없음 |
| `--filter <문자열>` | 이름에 해당 문자열이 포함된 시나리오만 실행 |
| `--preserve-test-containers` | 실행 후 컨테이너를 남겨둠 (디버깅용) |

`-i`와 `-u`는 **기본 테스트에만 적용됩니다** (`--help`의 표현은 "Not used for scenarios"). 시나리오는 `scenarios.json`에 적힌 `image`/`remoteUser`를 사용합니다.

`--skip-*`는 모두 기본값이 꺼져 있는 **제외용** 플래그입니다. 아무 옵션도 주지 않으면 기본 테스트와 시나리오가 모두 실행됩니다.

### 테스트 실행 조합

아래 명령은 모두 **저장소 루트에서 그대로 복사해 실행할 수 있습니다.** 각 조합이 만드는 컨테이너와 그 안에서 실행되는 스크립트를 함께 정리했습니다. 예시는 시나리오 3개를 가진 `color`를 기준으로 합니다.

**1. 특정 feature 전체**
```bash
devcontainer features test -p . -f color -i mcr.microsoft.com/devcontainers/base:ubuntu
```

| 컨테이너 | 설치되는 feature | 실행되는 스크립트 |
|---|---|---|
| 1개 | `color` | `test.sh` |
| 3개 (시나리오당 1개) | 각 시나리오의 `features` | `my_favorite_color_is_green.sh`, `gold.sh`, `green.sh` |

`test/_global/`의 전역 시나리오는 실행되지 않습니다. 특정 feature에 속하지 않기 때문입니다.

**2. 특정 feature의 기본 테스트만**
```bash
devcontainer features test -p . -f color --skip-scenarios -i mcr.microsoft.com/devcontainers/base:ubuntu
```

| 컨테이너 | 설치되는 feature | 실행되는 스크립트 |
|---|---|---|
| 1개 | `color` | `test.sh` |

`-i`와 `-u`는 이 컨테이너에만 적용됩니다.

**3. 특정 feature의 시나리오만**
```bash
devcontainer features test -p . -f color --skip-autogenerated --skip-duplicated
```

| 컨테이너 | 설치되는 feature | 실행되는 스크립트 |
|---|---|---|
| 3개 (시나리오당 1개) | 각 시나리오의 `features` | 각 `<scenario>.sh` |

기본 테스트를 이미 다른 곳에서 돌렸을 때 **중복 실행을 피하는 용도**입니다. 이 repo의 CI가 `test-autogenerated`와 `test-scenarios` job으로 이렇게 역할을 나눕니다.

**4. 전역 시나리오만**
```bash
devcontainer features test -p . --global-scenarios-only
```

| 컨테이너 | 설치되는 feature | 실행되는 스크립트 |
|---|---|---|
| 1개 | `color` + `hello` (전역 시나리오가 지정) | `color_and_hello.sh` |

`-f`와 함께 쓸 수 없습니다.

**5. 저장소 전체**
```bash
devcontainer features test -p . -i mcr.microsoft.com/devcontainers/base:ubuntu
```

| 컨테이너 | 설치되는 feature | 실행되는 스크립트 |
|---|---|---|
| 1개 | **모든 feature를 한꺼번에** | 각 feature의 `test.sh` 4개 |
| 7개 (시나리오당 1개) | 각 시나리오의 `features` | 각 `<scenario>.sh` (`arm-gnu-toolchain` 2, `color` 3, `hello` 1, `prezto` 1) |
| 2개 | `duplicate.sh`가 있는 feature를 **두 번씩** (`arm-gnu-toolchain`, `prezto`) | `duplicate.sh` |
| 1개 | 전역 시나리오가 지정 | `color_and_hello.sh` |

컨테이너 11개가 만들어집니다. 주의할 점이 둘 있습니다.

- 첫 번째 컨테이너는 **모든 feature가 함께 설치된 환경**이므로, `-f`로 하나씩 돌릴 때와 조건이 다릅니다. feature 간 간섭 문제는 이때만 드러날 수 있습니다
- 가장 오래 걸립니다. `arm-gnu-toolchain` 관련 컨테이너가 매번 수백 MB의 툴체인을 내려받습니다

### 피쳐 유닛 테스트 작성할 때 알아야 할 점

#### 1. 개발자는 `test.sh`만 작성하면 됩니다

`test/<feature>/test.sh`를 작성하면 컨테이너는 CLI가 알아서 준비합니다. 해당 feature 하나만 기본 옵션으로 넣은 `devcontainer.json`을 **자동 생성**해 컨테이너를 빌드하고, 그 안에서 `test.sh`를 실행합니다. `autogenerated`라는 이름은 테스트가 자동 생성된다는 뜻이 아니라 이 `devcontainer.json`이 자동 생성된다는 뜻입니다.

스크립트는 CLI가 제공하는 테스트 라이브러리를 source해서 `check`와 `reportResults`를 사용합니다.

```bash
#!/bin/bash
set +e
source dev-container-features-test-lib

check "<라벨>" <명령> [인자...]

reportResults
```

기본 테스트에는 feature 옵션을 지정할 방법이 없습니다. 옵션별 동작이나 다른 feature와의 조합을 검증하려면 `scenarios.json`에 `devcontainer.json`을 직접 작성해야 합니다. `prezto`는 옵션이 없지만, common-utils가 만든 유저 위에서 도는지 확인하려고 시나리오를 두고 있습니다.

#### 2. `set -e`를 쓰면 첫 실패에서 테스트 전체가 중단됩니다

라이브러리의 `check`는 실패를 배열에 모아두고 `return 1`을 하며, `reportResults`가 마지막에 그 목록을 보고합니다. `set -e`가 켜져 있으면 그 `return 1`이 스크립트를 즉시 종료시켜 **뒤의 체크가 하나도 실행되지 않고** `reportResults`도 실행되지 않습니다. CI 결과는 어느 쪽이든 실패이므로 차이는 한 번에 얻는 정보량입니다.

그래서 이 repo의 테스트 스크립트는 모두 `set +e`를 씁니다. upstream 템플릿([feature-starter](https://github.com/devcontainers/feature-starter/blob/main/test/hello/test.sh), [features](https://github.com/devcontainers/features/blob/main/test/git/test.sh))은 `set -e`를 쓰지만 체크가 1~3개뿐이라 차이가 드러나지 않습니다. upstream도 `set -e`를 전제로 라이브러리를 고친 적이 있습니다 ([devcontainers/cli#896](https://github.com/devcontainers/cli/pull/896)).

**주의**: `set +e`에서는 `check` 밖의 맨 명령(`apt-get install` 등)이 실패해도 그냥 넘어갑니다. 준비 단계는 `check`로 감싸거나 `|| exit 1`을 붙이세요.

#### 3. `install.sh`는 root, `test.sh`는 remoteUser로 실행됩니다

| | 실행 시점 | 실행 유저 |
|---|---|---|
| `install.sh` | 이미지 **빌드** 중 | **root** (uid 0) |
| `test.sh` | 컨테이너 **기동 후** | **remoteUser** (예: `vscode`, uid 1000) |

`install.sh`가 만든 파일은 그냥 두면 root 소유가 되어 정작 컨테이너를 쓰는 유저가 수정할 수 없습니다. CLI가 빌드 시점에 주입하는 변수를 써야 합니다. `install.sh` 안의 `$HOME`은 root의 홈(`/root`)이므로 쓰면 안 됩니다.

```bash
# install.sh — root로 실행됨
# _CONTAINER_USER=root, _CONTAINER_USER_HOME=/root
# _REMOTE_USER=vscode,  _REMOTE_USER_HOME=/home/vscode
sudo -u $_REMOTE_USER git clone --recursive https://... $_REMOTE_USER_HOME/.zprezto
```

반대로 `test.sh`에는 `_REMOTE_USER*`가 **없습니다.** 빌드 시점에만 주입되기 때문입니다. 대신 `test.sh`는 remoteUser 본인으로 실행되므로 `$(id -un)`이 `_REMOTE_USER`이고 `$HOME`이 `_REMOTE_USER_HOME`입니다.

```bash
# test.sh — remoteUser로 실행됨
check "no files owned by another user" bash -c '
    owner=$(id -un)
    stray=$(find "$HOME/.zprezto" ! -user "$owner" -print -quit)
    [ -z "$stray" ] || { echo "not owned by $owner: $stray"; exit 1; }
'
```

#### 4. 시나리오 이름과 테스트 스크립트 파일 이름은 반드시 같아야 합니다

`scenarios.json`의 키가 곧 실행할 스크립트의 파일명입니다. [공식 문서](https://github.com/devcontainers/cli/blob/main/docs/features/test.md)의 표현은 다음과 같습니다.

> The test command will build a container with the config above, and then look for a `.sh` test file with the same name.

`test/color/scenarios.json`에 `gold` 시나리오를 정의했다면 `test/color/gold.sh`가 있어야 합니다. 이름이 맞지 않으면 **에러가 아니라 조용히 건너뜁니다.** 시나리오 이름을 바꿀 때는 스크립트 파일명도 함께 바꿔야 합니다.

#### 5. `duplicate.sh` — 두 번 설치해도 괜찮은지 검증합니다

`test/<feature>/duplicate.sh`를 두면 CLI가 **같은 feature를 두 번 선언한** `devcontainer.json`을 만들어 컨테이너를 빌드하고 그 안에서 이 스크립트를 실행합니다. [공식 문서](https://github.com/devcontainers/cli/blob/main/docs/features/test.md)의 설명입니다.

> each Feature with a `duplicate.sh` will generate a test installing a given Feature twice (with different options). This is useful for asserting that a Feature can be installed multiple times without conflict (is idempotent).

옵션이 없는 feature에도 동작하며, 결과에는 `<feature> executed twice with randomized options`로 표시됩니다. 파일을 두지 않으면 조용히 건너뜁니다.

작성 방법은 `test.sh`와 같습니다. 다른 점은 **검증 대상이 "두 번 설치된 뒤의 상태"**라는 것뿐입니다. `install.sh`를 직접 다시 호출할 필요가 없습니다. 컨테이너가 이미 그 상태입니다.

```bash
#!/bin/bash
set +e
source dev-container-features-test-lib

# 두 번째 설치가 첫 번째 설치의 백업을 덮어쓰지 않았는지
check "backup not overwritten" bash -c '
    org="$HOME/.zprezto/runcoms/zpreztorc_org"
    new="$HOME/.zprezto/runcoms/zpreztorc"
    [ -f "$org" ] || { echo "backup missing"; exit 1; }
    diff -q "$org" "$new" >/dev/null && { echo "swap ran twice"; exit 1; }
    exit 0
'

reportResults
```

무엇을 검증할지는 `install.sh`가 **두 번 실행되면 깨질 수 있는 것**을 기준으로 고릅니다. 자주 나오는 것은 백업 파일 덮어쓰기, 설정 파일에 같은 줄이 두 번 추가되는 것(`>>`), 같은 경로에 서로 다른 버전이 섞여 설치되는 것입니다.

`test.sh`와 겹치는 체크는 공용 파일로 뽑아 쓸 수 있습니다. **`test/<feature>/` 안의 파일은 전부 컨테이너로 복사되므로** `source ./_common.sh` 같은 방식이 동작합니다.

## Feature 문서

`src/<feature>/README.md`는 **자동 생성물입니다.** 직접 고치면 릴리스 때 덮어써집니다. 손으로 쓸 내용은 같은 디렉토리의 `NOTES.md`에 넣으세요.

```
# <name> (<id>)                     ← devcontainer-feature.json의 name, id
<description>
## Example Usage
## Options                          ← options에서 생성한 표
─────────────────────────────
NOTES.md 내용이 여기 들어갑니다
─────────────────────────────
---
_Note: This file was auto-generated from the devcontainer-feature.json..._
```

다시 생성하려면:

```bash
make docs
```

feature 하나만 생성할 수는 없습니다. `generate-docs`에 feature를 고르는 옵션이 없어서 `-p`에 준 디렉토리의 하위 전체를 훑습니다. 다만 생성이 결정적이라 내용이 바뀐 feature의 README에만 diff가 생깁니다.

[release 워크플로우](.github/workflows/release.yaml)도 배포할 때 같은 문서를 만들어 PR로 올립니다. 미리 `make docs`를 돌려 커밋해 두면 그 PR이 비어 있게 됩니다.

Example Usage의 `:1`은 메이저 버전 태그입니다. publish하면 `1`, `1.0`, `1.0.0`, `latest`가 함께 올라가므로 소비자는 `:1`로 호환되는 업데이트를 받거나 `:1.0.0`으로 완전히 고정할 수 있습니다.

## TODO

### E. GitHub Actions 워크플로우 전반 리뷰

[test.yaml](.github/workflows/test.yaml), [release.yaml](.github/workflows/release.yaml), [validate.yml](.github/workflows/validate.yml) 세 파일 모두 `Initial commit`에 [devcontainers/feature-starter](https://github.com/devcontainers/feature-starter) 템플릿에서 그대로 들어온 뒤 검토된 적이 없습니다. 새 feature를 추가할 때 `test.yaml` 매트릭스를 갱신하지 않아 `prezto`와 `arm-gnu-toolchain`이 CI에서 빠져 있던 것도 그 결과입니다.

확인할 것:

- 테스트 명령이 deprecated된 위치 인자 형태입니다 (`... --skip-duplicated .`). 지금은 `.` 앞이 불리언 플래그라 정상 동작하지만 README는 `-p .`로 통일했습니다
- `release.yaml`이 `workflow_dispatch` 전용입니다. 수동 실행만 되는 것이 의도인지
- `validate.yml`이 `pull_request`와 `workflow_dispatch`에서만 돕니다. main에 직접 push할 때는 `devcontainer-feature.json` 검증이 없습니다
- 새 feature를 추가할 때 매트릭스를 함께 고치도록 남길 장치가 있는지
- 테스트 실행을 [Makefile](Makefile) 타깃으로 바꿀지. 세 job의 명령이 `make unit-<feature>`, `make scenario-<feature>`, `make test-global`과 그대로 대응하므로, 로컬과 CI가 갈라지지 않고 위의 deprecated 위치 인자 문제도 함께 사라집니다
